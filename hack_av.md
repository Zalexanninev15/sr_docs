# Особые переменные

{% hint style="info" %}
Временные переменные "живущие" лишь в рамках текущей сессии бота. Данные переменные не сохраняются при перезапуске Shark Remote, поэтому их необходимо задавать при каждом использовании Telegram бота.

Задавать переменные необходимо через команду /av.
{% endhint %}

1. Доступно 2 варианта печати для команды `/iprint`: автоопределение (всегда выбрано по умолчанию) и упрощённая печать. Чтобы сменить вариант печати: `/av iprintv1=true` для активации упрощённой печати или `/av iprintv1=false` для автоопределения.&#x20;
2. Переменная **sp** позволяющая регулировать количество мониторов на скриншотах. Пример использования:&#x20;

* `/av sp=-1` - текущий (устаревший) метод создания скриншота для обычного режима работы (оставлен для совместимости) и 1 монитор для службы
* `/av sp=0` - новый метод создания скриншота для 1 монитора для обычного режима работы и 1 монитор для службы
* `/av sp=1` - новый метод создания скриншота для 2+ мониторов для обычного режима работы и 2+ мониторов для службы. Работает только для команды `/snap` и команды `/screen` при отправке скриншота непосредственно в Telegram.

3. Переменная `umc` для выполнения скрытого Shell-кода. Требует особой осторожности при использовании.

{% hint style="danger" %}
Т.к. переменная umc несёт реальный риск для устройства и системы при использовании через команду /call, то начиная с 10 версии была добавлен флаг-ограничитель. [Подробнее](settings/flags.md).&#x20;
{% endhint %}

{% hint style="info" %}
В 6 версии был добавлен новый аргумент для вызова небезопасного Shell-кода из Telegram бота, что может быть удобно, в некоторых случаях.\
[Пример генерации кода в Kali Linux](https://github.com/Zalexanninev15/ShellCodeEx?tab=readme-ov-file#test-shellcode-need-powershell-7-on-windows). Для Kali Linux удобно использовать WSL в Windows 11 (имеется возможность установки WSL в Windows 10).\
Мой пример генерации кода:\
![](<.gitbook/assets/image (48).png>)\
Код: `msfconsole -x "use payload/windows/x64/exec; set CMD 'msg * You have been hacked :D'; generate -f ps1; exit"`\
В выводе выделен полученный Shell-код для выполнения в Telegram боте.\
\
Использование:\
Сначала задаём код в боте: `/av umc=[shell-код]`\
Затем пытаемся его выполнить: `/call umc`\
Демонстрация работы:\
![](<.gitbook/assets/image (49).png>)
{% endhint %}
